\documentclass[10pt,aspectratio=169]{beamer}

\usetheme{Madrid}
\usecolortheme{dolphin}
\usefonttheme{professionalfonts}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{tikz-cd}

\definecolor{deepblue}{RGB}{0,70,130}
\setbeamercolor{title}{fg=deepblue}
\setbeamercolor{frametitle}{fg=deepblue}
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{itemize items}[circle]

\newcommand{\code}[1]{\texttt{\small\detokenize{#1}}}

% Map macros, matching the manuscript (sections/chyll_v2.tex).
\newcommand{\Enc}{E_\theta}
\newcommand{\Dec}{D_\theta}
\newcommand{\Dyn}{F_\theta}
\newcommand{\Lat}{Z}
\newcommand{\Xr}{X'}

\title[CHyLL v2]{CHyLL v2: Continuous Latent Embeddings of Hybrid Systems}
\subtitle{Method, loss terms, and a guide to the trained models and data}
\author{Kaito Iwasaki}
\date{}

\begin{document}

\begin{frame}
  \titlepage
\end{frame}

\begin{frame}{Hybrid Dynamical Systems}
  A \emph{hybrid} dynamical system mixes smooth motion with sudden jumps.

  \vspace{0.8em}
  \begin{itemize}
    \item A passive walker: the swing leg moves smoothly, then strikes the
      ground and the state jumps.
    \item A bouncing ball: free flight under gravity, then an instantaneous
      change of velocity at each impact.
  \end{itemize}

  \vspace{0.8em}
  The jump is a genuine discontinuity. A smooth model of the dynamics cannot
  be fitted directly across it.
\end{frame}

\begin{frame}{Three Example Systems}
  \begin{center}
  \begin{tabular}{lcl}
    \toprule
    System & State dim. & Behavior \\
    \midrule
    Rimless wheel & 2 & stable rolling limit cycle \\
    Bouncing ball & 2 & bounces, losing energy at each impact \\
    Compass-gait biped & 4 & period-doubling route to chaos \\
    \bottomrule
  \end{tabular}
  \end{center}

  \vspace{1em}
  They increase in difficulty. The compass-gait biped walks with period 1,
  then 2, 4, 8, and finally chaotically as the ground slope steepens.
\end{frame}

\begin{frame}{The Idea}
  Replace the discontinuous system with a continuous one that is easy to model.

  \vspace{0.8em}
  \begin{itemize}
    \item An \textbf{encoder} maps each state into a latent space.
    \item A single \textbf{smooth vector field} advances the latent state in
      time.
    \item A \textbf{decoder} maps the latent state back.
  \end{itemize}

  \vspace{0.8em}
  The jump is absorbed into the smooth latent flow. This follows Teng, Liu,
  and Sreenath (ICML 2026).
\end{frame}

\begin{frame}{The Mapping-Cylinder State Space}
  To make the jump continuous, the state is augmented with one extra
  coordinate \(s \in [0,1]\):
  \[
    y = (x, s) \in X'.
  \]

  \begin{itemize}
    \item \(s = 0\): ordinary motion of the original system.
    \item \(s\) sweeping \(0 \to 1\): the jump, unrolled as a continuous path.
  \end{itemize}

  \vspace{0.6em}
  On \(X'\) the trajectory is continuous, so a smooth latent model can be fitted
  to it.
\end{frame}

\begin{frame}{The Learned Maps}
  CHyLL v2 learns three maps. Write \(Z = \mathbb{R}^d\) for the latent space.

  \vspace{0.6em}
  \begin{align*}
    E_\theta &: X' \to Z && \text{encoder},\\
    V_\theta &: Z \to Z && \text{latent vector field},\\
    D_\theta &: Z \to X' && \text{decoder}.
  \end{align*}

  \vspace{0.4em}
  The time-\(\tau\) flow of \(V_\theta\) is written \(F_\theta\). The latent
  dimension is \(d = 7\) for the rimless wheel and bouncing ball, and
  \(d = 11\) for the compass-gait biped.
\end{frame}

\begin{frame}{Training: The Forward Pass}
  For a sampled trajectory slice \(y_0, \ldots, y_{T-1}\):

  \vspace{0.5em}
  \begin{align*}
    z_i &= E_\theta(y_i),\\
    \widehat z_i &= F_\theta^{\,i}(z_0),\\
    \widehat y_i &= D_\theta(\widehat z_i).
  \end{align*}

  \vspace{0.4em}
  Each state is encoded, the latent flow is integrated forward, and the
  integrated latent trajectory is decoded. The loss compares the encoded
  \(z_i\), the integrated \(\widehat z_i\), and the decoded \(\widehat y_i\)
  against the data.
\end{frame}

\begin{frame}[fragile]{The Commutative Diagram}
  Here \(f_\tau\) is one time-\(\tau\) step of the system, \(G\) the impact
  set, \(r\) the reset map, and \(\iota_0, \iota_1\) the inclusions at
  \(s = 0\) and \(s = 1\). The learned maps should make this diagram commute:

  \vspace{0.3em}
  \begin{center}
  \small
  \begin{tikzcd}[column sep=3.0em, row sep=2.4em]
    G \arrow[r, "\iota_1"] \arrow[d, "\iota_0 \circ r"']
    & \Xr \arrow[r, "f_\tau"] \arrow[d, "\Enc"']
    & \Xr \arrow[d, "\Enc"] \\
    \Xr \arrow[r, "\Enc"'] \arrow[rd, "\mathrm{id}_{\Xr}"']
    & \Lat \arrow[r, "\Dyn"'] \arrow[d, "\Dec"']
    & \Lat \\
    & \Xr
    \arrow[phantom, from=2-1, to=3-2]
    &
  \end{tikzcd}
  \end{center}

  \vspace{0.2em}
  \normalsize
  Each cell is enforced by one loss term:
  \begin{itemize}
    \item Left square, \(L_g\): \(E_\theta\) sends both sides of a reset to one latent point.
    \item Top-right square, \(L_z\): the latent flow matches the dynamics.
    \item Lower triangle, \(L_x\): the decoder inverts the encoder.
  \end{itemize}
\end{frame}

\begin{frame}{The Diagram Loss Terms}
  \small
  Write \(z_i = E_\theta(y_i)\) and \(\widehat z_i = F_\theta^{\,i}(z_0)\). The
  sampled data satisfies \(y_{i+1} = f_\tau(y_i)\).

  \medskip
  \textbf{Latent consistency.}\quad
  \(\displaystyle L_z = \frac{1}{T}\sum_i \lVert \widehat z_i - E_\theta(y_i)\rVert^2\)

  \(L_z = 0 \;\Rightarrow\; F_\theta \circ E_\theta = E_\theta \circ f_\tau\),
  the semi-conjugacy square.

  \medskip
  \textbf{Reconstruction.}\quad
  \(\displaystyle L_x = \frac{1}{T}\sum_i \lVert D_\theta(\widehat z_i) - y_i\rVert^2\)

  \(L_z = L_x = 0 \;\Rightarrow\; D_\theta \circ E_\theta = \mathrm{id}_{X'}\),
  the reconstruction triangle.

  \medskip
  \textbf{Gluing.}\quad
  \(\displaystyle L_g = \frac{1}{|\mathcal{G}|}\sum_{k\in\mathcal{G}}
    \lVert E_\theta(y_{k+1}) - E_\theta(y_k)\rVert^2\)

  \(L_g = 0 \;\Rightarrow\; E_\theta\circ\iota_1 = E_\theta\circ\iota_0\circ r\)
  on \(G\), the gluing square.
\end{frame}

\begin{frame}{The Regularity Loss Terms}
  \small
  \(L_v\) and \(L_c\) are not diagram cells. They keep the embedding
  well-behaved.

  \medskip
  \textbf{Seam velocity.}\quad The seam set \(\mathcal{S}\) is the union of
  the cylinder-entry seams and the gluing-exit seams. With
  \(z_k = E_\theta(y_k)\) and step \(\tau\),
  \[ L_v = \frac{1}{|\mathcal{S}|}\sum_{k\in\mathcal{S}}
     \Big\lVert \frac{z_k - z_{k-1}}{\tau}
       - \frac{z_{k+2} - z_{k+1}}{\tau}\Big\rVert^2 . \]
  Minimizing \(L_v\) brings the latent velocity just before and just after
  each seam into agreement, the \(C^1\) (no-kink) condition the loss aims to
  enforce.

  \medskip
  \textbf{Anti-collapse.}\quad With \(\sigma_j^2\) the variance of latent
  coordinate \(j\) and floor \(\lambda\),
  \(\displaystyle L_c = \sum_{j=1}^{d}\max(0,\ \lambda - \sigma_j^2)\).
  Any coordinate with variance below \(\lambda\) is penalized, so \(E_\theta\)
  keeps all \(d\) coordinates active.

  \medskip
  The edge sets \(\mathcal{G}\) and \(\mathcal{S}\) are detected from the
  sampled coordinate \(s\), not from symbolic guard equations.
\end{frame}

\begin{frame}{The Composite Objective}
  \[
    L = w_x L_x + w_z L_z + w_g L_g + w_v L_v + w_c L_c.
  \]

  \vspace{0.8em}
  \begin{center}
  \begin{tabular}{lc}
    \toprule
    Setting & \((w_x, w_z, w_g, w_v, w_c)\) \\
    \midrule
    Rimless wheel, bouncing ball & \((1,\ 1,\ 3,\ 1,\ 1)\) \\
    Compass-gait biped & \((1,\ 1,\ 3,\ 0,\ 1)\) \\
    \bottomrule
  \end{tabular}
  \end{center}

  \vspace{0.8em}
  The exact values for any run are stored in its \code{config.json}.
\end{frame}

\begin{frame}{Training: Rimless Wheel}
  Training has two phases: Phase A from a random start, then Phase B
  fine-tuning with the seam-velocity term \(L_v\). The loss falls smoothly
  across the curriculum and the rollout tracks the true trajectory through
  many impacts.

  \begin{center}%
  \includegraphics[height=3.5cm]{../figures/rimless_wheel_phaseB_finetune/loss_history.png}%
  \hspace{1.5em}%
  \includegraphics[height=3.5cm]{../figures/rimless_wheel_phaseB_finetune/rollout_vs_truth.png}%
  \end{center}

  {\small Left: loss history. Right: decoded rollout against ground truth.
  Final composite loss \(\approx 3.7\times10^{-3}\).}
\end{frame}

\begin{frame}{Training: Bouncing Ball}
  The bouncing ball uses the same two-phase training. Each impact removes
  energy, so the true trajectory is a decaying sequence of bounces, which the
  rollout reproduces.

  \begin{center}%
  \includegraphics[height=3.5cm]{../figures/bouncing_ball_phaseB_finetune/loss_history.png}%
  \hspace{1.5em}%
  \includegraphics[height=3.5cm]{../figures/bouncing_ball_phaseB_finetune/rollout_vs_truth.png}%
  \end{center}

  {\small Left: loss history. Right: decoded rollout against ground truth.
  Final composite loss \(\approx 6.8\times10^{-3}\).}
\end{frame}

\begin{frame}{Training: Compass-Gait Biped}
  The compass-gait biped trains in a single phase, one run per ground slope.
  Shown is the period-2 gait at \(4.75^\circ\); the steeper cascade slopes
  train the same way.

  \begin{columns}[c]
    \begin{column}{0.60\textwidth}
      \centering
      \includegraphics[height=3.0cm]{../figures/compass_gait_phi_1_4.75deg/loss_history.png}\\
      {\small loss history}
    \end{column}
    \begin{column}{0.38\textwidth}
      \centering
      \includegraphics[height=4.5cm]{../figures/compass_gait_phi_1_4.75deg/rollout_vs_truth.png}\\
      {\small rollout vs.\ truth}
    \end{column}
  \end{columns}

  \vspace{0.3em}
  {\small Final composite loss \(\approx 1.0\times10^{-2}\). The rollout panel
  shows the augmented state, four physical variables and the coordinate \(s\).}
\end{frame}

\begin{frame}{Results}
  \begin{itemize}
    \item \textbf{Rimless wheel.} The model reproduces the rolling limit cycle,
      and the learned embedding recovers its expected single topological cycle.
    \item \textbf{Compass-gait biped.} The cascade analysis recovers the
      period-doubling sequence exactly: the learned latent states form 2, 4,
      and 8 clusters at the period-2, -4, and -8 gaits, and disperse under
      chaos.
    \item \textbf{Bouncing ball.} The orbit spirals slowly toward rest rather
      than closing into a loop, so its topological signature is only partially
      recovered.
  \end{itemize}
\end{frame}

\begin{frame}{Where the Models and Data Live}
  \small
  \begin{center}
  \begin{tabular}{ll}
    \toprule
    Path & Contents \\
    \midrule
    \code{runs/<name>/} & trained model, config, training log \\
    \code{figures/<name>/} & loss and rollout figures \\
    \code{cycling_signature/data/} & exported lifts and rank summaries \\
    \code{cycling_signature/figures/} & rank distribution and heatmap figures \\
    \code{compass_analysis/} & compass cascade cluster analysis \\
    \bottomrule
  \end{tabular}
  \end{center}

  \vspace{0.7em}
  \normalsize
  Paths are under \code{chyll_v2/}. Each \code{<name>} is one trained run,
  for example \code{rimless_wheel_phaseB_finetune}.
\end{frame}

\begin{frame}{A Trained Run}
  Each directory in \code{chyll_v2/runs/} contains:

  \vspace{0.5em}
  \begin{itemize}
    \item \code{model.pt}: the learned network weights.
    \item \code{config.json}: dimensions, system parameters, and loss weights.
    \item \code{train_log.jsonl}: the loss terms recorded over training.
  \end{itemize}

  \vspace{0.6em}
  The config is enough to rebuild the network and reload the weights. The
  export scripts under \code{cycling_signature/export/} do exactly this.
\end{frame}

\begin{frame}{What an Exported Lift Contains}
  An exported lift is a sampled trajectory in the latent space \(Z\):

  \vspace{0.5em}
  \begin{itemize}
    \item \code{..._positions}: the sampled latent points, one per row.
    \item \code{..._tangents}: the unit tangent direction at each point.
  \end{itemize}

  \vspace{0.5em}
  Both arrays have shape \((N, d)\): \(N\) samples and \(d\) latent
  coordinates. Each lift is written as \code{.npy} for Python and \code{.csv}
  for inspection.
\end{frame}

\begin{frame}[fragile]{Loading a Lift in Python}
\begin{verbatim}
import numpy as np

folder = "chyll_v2/cycling_signature/data/rimless_wheel"
base = folder + "/continuous_lift_chyll_v2_phaseB"

Z = np.load(base + ".npy")           # positions
T = np.load(base + "_tangents.npy")  # unit tangents

print(Z.shape, T.shape)
print(np.linalg.norm(T, axis=1).max())
\end{verbatim}

  \code{Z} holds the latent positions and \code{T} the unit tangents. The
  printed tangent norm should be close to \(1\).
\end{frame}

\begin{frame}{From a Trained Model to a Rank Figure}
  The topology analysis runs in three stages:

  \vspace{0.5em}
  \begin{enumerate}
    \item \textbf{Export}: a script in \code{cycling_signature/export/} turns
      a trained model into a latent lift.
    \item \textbf{Compute}: \code{cycling_signature/julia/run_subsegments.jl}
      computes the cycling signature over random subsegments.
    \item \textbf{Plot}: \code{cycling_signature/plot/plot_signature_figures.py}
      draws the rank distribution and heatmaps.
  \end{enumerate}

  \vspace{0.5em}
  The \code{cycling_signature/} folder has its own README with the exact
  commands.
\end{frame}

\begin{frame}{Sanity Checks}
  After loading a lift:

  \vspace{0.5em}
  \begin{itemize}
    \item positions and tangents have the same shape,
    \item tangent norms are close to \(1\),
    \item the latent dimension \(d\) matches the system, 7 or 11.
  \end{itemize}

  \vspace{0.6em}
  A common mistake is pairing a model with the wrong \code{config.json}: the
  network shape will not match the saved weights. Take \code{model.pt} and
  \code{config.json} from the same run directory.
\end{frame}

\begin{frame}{Where the Code Lives}
  \small
  \begin{center}
  \begin{tabular}{ll}
    \toprule
    Object & File \\
    \midrule
    \(E_\theta, V_\theta, D_\theta\) & \code{networks.py} \\
    latent ODE rollout & \code{ode.py} \\
    loss terms \(L_x, L_z, L_g, L_v, L_c\) & \code{losses.py} \\
    training loop & \code{train.py} \\
    system simulators and reset maps & \code{systems/} \\
    \bottomrule
  \end{tabular}
  \end{center}

  \vspace{0.7em}
  \normalsize
  These files are the CHyLL v2 core library, under \code{chyll_v2/chyll_v2/}.
  Topology postprocessing lives under \code{chyll_v2/cycling_signature/}.
\end{frame}

\begin{frame}{Summary}
  \begin{itemize}
    \item A hybrid system is made continuous on the mapping cylinder \(X'\).
    \item CHyLL v2 learns an encoder, a latent vector field, and a decoder.
    \item \(L_g\), \(L_z\), and \(L_x\) are the commuting cells of the diagram.
    \item \(L_v\) and \(L_c\) add the seam-regularity conditions.
    \item The learned embeddings recover the expected topology for the rimless
      wheel and the compass-gait cascade.
  \end{itemize}
\end{frame}

\end{document}
